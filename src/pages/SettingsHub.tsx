import { Moon, Settings, Sun } from 'lucide-react'
import { PageHeader } from '../components/ui/PageHeader'
import { useTheme, type ThemeMode } from '../context/ThemeContext'
export function SettingsHub() { const { mode, setMode } = useTheme(); const options: { value: ThemeMode; label: string }[] = [{ value: 'light', label: 'فاتح' }, { value: 'dark', label: 'داكن' }, { value: 'system', label: 'حسب الجهاز' }]; return <div className="page"><PageHeader title="إعدادات النظام" description="تفضيلات العرض المحلية لهذا الجهاز. إعدادات السياسات المالية تُدار بعقود مستقلة ومعتمدة." /><section className="panel settings-panel"><header className="panel-header"><h2>المظهر</h2><Settings size={18} /></header><div className="panel-body"><div className="segmented">{options.map((option) => <button key={option.value} className={mode === option.value ? 'active' : ''} onClick={() => setMode(option.value)}>{option.value === 'dark' ? <Moon size={15} /> : <Sun size={15} />}{option.label}</button>)}</div></div></section></div> }
