import { KeyRound, ShieldCheck, UserRound } from 'lucide-react'
import { PageHeader } from '../components/ui/PageHeader'
import { useAuth } from '../context/AuthContext'
import { formatDate } from '../lib/format'
export function AccessPage() { const { access } = useAuth(); return <div className="page"><PageHeader title="الحساب والصلاحيات" description="السياق المعروض صادر من تعيينات قاعدة البيانات النشطة، وليس من بيانات وصفية قابلة للتعديل." /><div className="content-grid"><section className="panel"><header className="panel-header"><h2>هوية العمل</h2><UserRound size={18} /></header><div className="panel-body"><dl className="definition-list"><div><dt>الاسم</dt><dd>{access?.display_name}</dd></div><div><dt>المؤسسة</dt><dd>{access?.organization_name}</dd></div><div><dt>الكود</dt><dd>{access?.organization_code}</dd></div><div><dt>العملة</dt><dd>{access?.currency_code}</dd></div><div><dt>المنطقة الزمنية</dt><dd>{access?.timezone_name}</dd></div><div><dt>آخر تحقق</dt><dd>{formatDate(access?.generated_at, true)}</dd></div></dl></div></section><section className="panel"><header className="panel-header"><h2>الأدوار والصلاحيات</h2><ShieldCheck size={18} /></header><div className="panel-body"><h3 className="compact-title"><KeyRound size={16} />الأدوار</h3><div className="chip-list">{access?.role_keys.map((role) => <span key={role}>{role}</span>)}</div><h3 className="compact-title">الصلاحيات</h3><div className="chip-list">{access?.permission_keys.map((permission) => <span key={permission}>{permission}</span>)}</div></div></section></div></div> }
