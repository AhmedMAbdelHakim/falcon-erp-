# Administrator Guide

Administrators activate users, assign organization-scoped roles, review environment state and investigate audit evidence. Grant the narrowest role required, use dated assignment reasons and remove expired access. Authentication does not itself authorize a workflow.

Never place privileged keys in the browser, alter posted accounting rows, reassign immutable profile organization, or bypass command RPCs. For incidents preserve correlation IDs and follow the production runbook.
